require 'spec_helper'

RSpec.describe ImageCataloguer::PageBuilder do
  describe "generate_catalogue" do
    before (:each) do
      @images = build_list(:image, 10)
    end

    it "should do something" do
    end

  end
end